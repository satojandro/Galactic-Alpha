{
  "name": "galactic-vault-contracts",
  "version": "1.0.0",
  "description": "Galactic Alpha smart contracts - ERC4626 vault with cosmic alignment",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:zircuit": "hardhat run scripts/deploy.js --network zircuit",
    "deploy:zircuit-testnet": "hardhat run scripts/deploy.js --network zircuitTestnet",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0",
    "dotenv": "^16.3.1"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  }
}

