/**
 * Example vault configuration
 * 
 * Copy this file to vault-config.ts and add your deployed vault addresses.
 * Make sure vault-config.ts is in .gitignore to keep addresses private.
 */

export const VAULT_CONFIG = {
  // Example: Add your deployed vault addresses here
  // "mercury-retrograde": "0x..." as `0x${string}`,
  // "full-moon-growth": "0x..." as `0x${string}`,
  // etc.
}

